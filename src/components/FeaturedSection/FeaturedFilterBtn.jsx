export default function FeaturedFilterBtn ({ category, link }) {
    return (
            <a href="#" link={link} className="btn btn-blue-inverted">
                {category}
            </a>
    )
}